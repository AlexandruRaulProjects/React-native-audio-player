class User {
    constructor(
        id,
        fullName,
        username,
        email,
        password,
    ) {
        this.id = id;
        this.fullName = fullName;
        this.username = username;
        this.email = email;
        this.password = password;
    }
}

export default User;

